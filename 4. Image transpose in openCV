import cv2
import numpy as np

img = cv2.imread('dog.jpg')

#transpose directly 90 degree
rotated_image = cv2.transpose(img)
cv2.imshow('rotated',rotated_image)
cv2.imshow('original',img)
cv2.waitKey(0)
cv2.destroyAllWindows()
