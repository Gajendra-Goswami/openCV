#interplotation technoque
import cv2
import numpy as np

img = cv2.imread('dog.jpg')





cv2.imshow('original',img)
cv2.waitKey(0)


img_scaled = cv2.resize(img, None, fx=0.75, fy=0.75)
cv2.imshow("scaling",img_scaled)
cv2.waitKey(0)
cv2.destroyAllWindows()
