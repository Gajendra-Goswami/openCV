import cv2

cap=cv2.VideoCapture(0)
while True:

    ret,frame=cap.read()
    cv2.imshow("liuve sketch",frame)
    if cv2.waitKey(1)==13:
        #13 ascci of enter agr enter dbaya to y lop ko break krega
        break

cap.release()
cv2.destroyAllWindows()
